{
  "name": "@ocean-app/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "ocean",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "react-app:build": "yarn workspace @ocean-app/react-app build",
    "react-app:eject": "yarn workspace @ocean-app/react-app eject",
    "react-app:start": "yarn workspace @ocean-app/react-app start",
    "react-app:test": "yarn workspace @ocean-app/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}